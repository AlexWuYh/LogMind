version: '3.8'

services:
  logmind:
    build:
      context: .
      dockerfile: Dockerfile.cn  ## 使用国内源进行镜像构建
    container_name: logmind-app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    # depends_on:
    #   postgres:
    #     condition: service_healthy
    # For SQLite: use named volume to avoid permission issues
    volumes:
      - sqlite_data:/app/prisma

  # postgres:
  #   image: postgres:15-alpine
  #   container_name: logmind-db
  #   restart: always
  #   environment:
  #     - POSTGRES_USER=logmind
  #     - POSTGRES_PASSWORD=logmind_password
  #     - POSTGRES_DB=logmind_db
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U logmind -d logmind_db"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

# volumes:
#   postgres_data:

volumes:
  sqlite_data:
