# LogMind Environment Variables
# Copy this file to .env and modify values as needed

# ============================================
# Database Configuration
# ============================================
# Choose ONE of the following database types:

# SQLite (development):
# DATABASE_URL=file:./dev.db

# PostgreSQL (production):
# DATABASE_URL=postgresql://logmind:logmind_password@postgres:5432/logmind_db

# PostgreSQL Credentials (used by docker-compose for postgres service):
POSTGRES_USER=logmind
POSTGRES_PASSWORD=logmind_password
POSTGRES_DB=logmind_db

# ============================================
# NextAuth Configuration
# ============================================
# IMPORTANT: Change this to your actual server IP or domain for production
NEXTAUTH_URL=http://localhost:3000

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-nextauth-key-change-this

# Trust host for NextAuth in production behind proxy
AUTH_TRUST_HOST=true
